version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - uploads:/app/uploads
      - jobs:/app/jobs
      - output:/app/output
    environment:
      - MAX_CONCURRENT_JOBS=3
      - JOB_EXPIRY_SECONDS=3600
      - RENDERIQ_ENV=production
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  uploads:
  jobs:
  output:
